{
  "scripts": {
    "clean": "rmdir /s /q dist",
    "build": "npm run clean && npx tsc --project tsconfig.json --watch",
    "build-css": "sass --watch ./style/main.scss ./style/style.css"
  },
  "dependencies": {
    "sass": "^1.89.2"
  }
}
